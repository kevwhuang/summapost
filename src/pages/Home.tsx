import React from 'react';

import Form from '../components/Form';

function Home(): React.ReactElement {
    return (
        <main id="home">
            <Form />
        </main>
    );
}

export default Home;

import React from 'react';

import Info from '../components/Info';

function Help(): React.ReactElement {
    return (
        <main id="help">
            <Info />
        </main>
    );
}

export default Help;
